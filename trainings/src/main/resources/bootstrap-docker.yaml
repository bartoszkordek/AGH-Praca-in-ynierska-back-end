spring:
  cloud:
    config:
      name: trainings
      discovery:
        service-id: config-server
        enabled: true

eureka:
  client:
    region: default
    registry-fetch-interval-seconds: 5
    service-url:
      defaultZone: http://discovery:8010/eureka