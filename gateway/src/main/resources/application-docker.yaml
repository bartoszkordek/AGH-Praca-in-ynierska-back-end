spring:
  rabbitmq:
    host: rabbitmq
  zipkin:
    base-url: http://zipkin:9411

eureka:
  client:
    service-url:
      defaultZone: http://discovery:8010/eureka

logging:
  file:
    path: /logs

#custom settings
front-end:
  url: http://localhost:5000